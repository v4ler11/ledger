[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "scripts"
version = "0.1.0"
description = "Dev scripts for the ledger workspace."
requires-python = ">=3.12"
dependencies = ["tabulate>=0.9"]

[project.scripts]
count_mcp_tools_tokens = "scripts.count_mcp_tools_tokens:main"