[project]
name = "ledgerd"
version = "0.1.1"
description = "Single-writer / multi-reader git sync of ledger files with daily squashed commits"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
